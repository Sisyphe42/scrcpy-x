# Problems
- TypeScript + Vue import paths may cause linting/type-check issues if existing project structure differs (App.vue, i18n setup, styles.css presence).
- Dynamic imports require correct vite config and alias resolution; ensure aliases match tsconfig paths.
