# Decisions
- Implement LaunchView.vue as a main action UI that composes DeviceList, SessionOptionsForm, ProfileList.
- Use single-device launch for MVP; multi-device launch remains out of scope for this task.
- Add a quick profile dropdown UI (UI placeholder) for future integration with profile switching.
- Ensure the Launch button is disabled until a device is selected; provide inline status messages for user feedback.
