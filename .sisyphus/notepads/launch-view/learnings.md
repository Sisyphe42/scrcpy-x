# Learnings
- Phase 1 analysis required: explore design system and existing components before implementing UI (DeviceList, SessionOptionsForm, ProfileList).
- Use Naive UI components consistently (n-space, n-card, n-button, n-select) to ensure visual cohesion.
- When store shapes are unclear, prefer reading existing stores and patterns to avoid guessing.
