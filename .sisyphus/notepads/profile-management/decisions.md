# Decisions
- Chose Naive UI components with a minimal Profile Editor in a modal to reduce navigation.
- Implemented SessionOptionsForm as a JSON editor to store profile options, allowing flexible option schemas without changing store types.
- Centralized profile CRUD in profileStore to keep UI lean and data consistent.
